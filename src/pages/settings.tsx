import { Layout } from "components/templates";

const SettingsPage = () => {
  return (
    <Layout title="Settings Page" subtitle="We are build its content yet">
      <h3>Content</h3>
    </Layout>
  );
};

export default SettingsPage;
